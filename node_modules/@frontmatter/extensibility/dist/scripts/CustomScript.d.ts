import { Question } from "../models/Question.js";
export declare abstract class CustomScript {
    /**
     * Call this method to ask questions to the user
     * @param questions
     */
    static askQuestions(questions: Question[]): void;
    /**
     * Call this method to indicate that the script has finished.
     * @param log
     */
    static done(log: string): void;
}
